# Directory Copy

## One-line tagline

Connect OpenEvidence to AI agents through your own authenticated browser session.

## Short description

OpenEvidence MCP is an unofficial Model Context Protocol server for Codex, Claude, Cursor, Cline, Continue, and other MCP-compatible clients. It uses the user's own authenticated OpenEvidence browser session and does not require an official API token.

## Long description

OpenEvidence MCP provides a local stdio MCP server with tools for checking auth status, listing prior OpenEvidence history, fetching article payloads by ID, and asking OpenEvidence research questions with optional completion polling. It is intended for users who already have an OpenEvidence account and want to connect their own session to MCP-compatible AI tools.

This project is unofficial and is not affiliated with OpenEvidence. It does not provide medical advice, does not bypass authentication or access controls, and does not collect credentials. Users are responsible for protecting local session state and following applicable terms, privacy rules, and clinical governance requirements.

## Keywords

`mcp`, `model-context-protocol`, `openevidence`, `medical-ai`, `healthcare-ai`, `clinical-research`, `ai-agents`, `codex`, `claude`, `cursor`, `cline`, `continue`, `playwright`, `typescript`

## GitHub About

Unofficial MCP server connecting OpenEvidence to Codex, Claude, Cursor, Cline, Continue, and other AI agents via authenticated browser session.

## X / LinkedIn post

OpenEvidence MCP is an unofficial MCP server that connects OpenEvidence to Codex, Claude, Cursor, Cline, Continue, and other MCP clients through your own authenticated browser session.

It includes tools for auth status, history, article fetch, and research questions with optional polling. No official API token required. Not affiliated with OpenEvidence and not medical advice.

## Русский

Неофициальный MCP-сервер для подключения OpenEvidence к Codex, Claude, Cursor, Cline, Continue и другим MCP-клиентам через вашу собственную авторизованную browser session. Не является медицинской консультацией и не обходит контроль доступа.

## Español

Servidor MCP no oficial para conectar OpenEvidence con Codex, Claude, Cursor, Cline, Continue y otros clientes MCP mediante tu propia sesión autenticada del navegador. No ofrece consejo médico ni evita controles de acceso.

## 中文

非官方 MCP 服务器，通过你自己的已认证浏览器会话将 OpenEvidence 连接到 Codex、Claude、Cursor、Cline、Continue 和其他 MCP 客户端。不提供医疗建议，也不绕过访问控制。

## हिन्दी

OpenEvidence को Codex, Claude, Cursor, Cline, Continue और अन्य MCP clients से जोड़ने वाला unofficial MCP server, जो आपके अपने authenticated browser session का उपयोग करता है। यह medical advice नहीं है और access controls bypass नहीं करता।
